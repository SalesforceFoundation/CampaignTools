project:
    name: CampaignTools
    package:
        name: Campaign Tools
        namespace: camptools
        api_version: 37.0
    git:
        prefix_release: rel/

flows:
    ci_feature:
        tasks:
            # CumulusCI's debug log parser errors if using test data loads, so don't parse debug logs on tests
            9:
                task: run_tests
